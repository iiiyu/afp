# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# The Opportunities + Apps core needs no seed data: opportunities live in the
# configured external repo, and apps are added through the /apps surface.
